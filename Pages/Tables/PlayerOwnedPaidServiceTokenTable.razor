@page "/tables/playerownedpaidservicetoken"

@inject YourGameServer.Data.GameDbContext dbContext
@inject MudBlazor.IScrollManager scrollManager
@inject MudBlazor.IDialogService dialogService
@inject MudBlazor.ISnackbar snackBar

<MudExpansionPanel id="@EditPanelId" Text="@EditPanelText" @bind-IsExpanded="_editPanelExpanded">
    <EditForm Model="@_newItem" OnValidSubmit="OnValidSubmit">
        <DataAnnotationsValidator />
        <MudCard>
            <MudCardContent>
                <MudTd>
                    <MudNumericField @bind-Value="_newItem.OwnerId" Label="@(TextHelper.LabelFor<PlayerOwnedPaidServiceToken>(i => i.OwnerId))" T="ulong" Min="0" />
                </MudTd>
                <MudTd>
                    <MudNumericField @bind-Value="_newItem.ServiceTokenId" Label="@(TextHelper.LabelFor<PlayerOwnedPaidServiceToken>(i => i.ServiceTokenId))" T="ulong" Min="0" />
                </MudTd>
                <MudTd>
                    <MudSelect T="ConsumableOrigin" @bind-Value="_newItem.Origin" Label="@(TextHelper.LabelFor<PlayerOwnedPaidServiceToken>(i => i.Origin))">
                        @foreach (ConsumableOrigin item in Enum.GetValues(typeof(ConsumableOrigin)))
                        {
                            <MudSelectItem Value="@item">@item.GetDisplayName()</MudSelectItem>
                        }
                    </MudSelect>
                </MudTd>
                <MudTd>
                    <MudSelect T="ConsumableStatus" @bind-Value="_newItem.Status" Label="@(TextHelper.LabelFor<PlayerOwnedPaidServiceToken>(i => i.Status))">
                        @foreach (ConsumableStatus item in Enum.GetValues(typeof(ConsumableStatus)))
                        {
                            <MudSelectItem Value="@item">@item.GetDisplayName()</MudSelectItem>
                        }
                    </MudSelect>
                </MudTd>
                <MudTd>
                    <MudDatePicker @bind-Date="_newItem.Period" Label="@(TextHelper.LabelFor<PlayerOwnedPaidServiceToken>(i => i.Period))" />
                    <MudTimePicker Time="_newItem.Period?.TimeOfDay" Label="Period" TimeChanged="(t) => _newItem.Period = _newItem.Period?.Date + t" />
                </MudTd>
                <MudTd>
                    <MudDatePicker @bind-Date="_newItem.UsedDate" Label="@(TextHelper.LabelFor<PlayerOwnedPaidServiceToken>(i => i.UsedDate))" />
                    <MudTimePicker Time="_newItem.UsedDate?.TimeOfDay" Label="UsedDate" TimeChanged="(t) => _newItem.UsedDate = _newItem.UsedDate?.Date + t" />
                </MudTd>
                <MudTd>
                    <MudDatePicker @bind-Date="_newItem.InvalidateDate" Label="@(TextHelper.LabelFor<PlayerOwnedPaidServiceToken>(i => i.InvalidateDate))" />
                    <MudTimePicker Time="_newItem.InvalidateDate?.TimeOfDay" Label="InvalidateDate" TimeChanged="(t) => _newItem.InvalidateDate = _newItem.InvalidateDate?.Date + t" />
                </MudTd>
                <MudTd>
                    <MudDatePicker @bind-Date="_newItem.ExpireDate" Label="@(TextHelper.LabelFor<PlayerOwnedPaidServiceToken>(i => i.ExpireDate))" />
                    <MudTimePicker Time="_newItem.ExpireDate?.TimeOfDay" Label="ExpireDate" TimeChanged="(t) => _newItem.ExpireDate = _newItem.ExpireDate?.Date + t" />
                </MudTd>
            </MudCardContent>
            <MudCardActions>
                <MudButton ButtonType="ButtonType.Submit" Variant="Variant.Filled" Color="Color.Success">Create New</MudButton>
                <MudButton Class="ml-2" Variant="Variant.Filled" OnClick="Clear">Clear</MudButton>
            </MudCardActions>
        </MudCard>
    </EditForm>
</MudExpansionPanel>
<br />
<MudDataGrid Items="dbContext.PlayerOwnedPaidServiceTokens" Sortable="true" Filterable="true" QuickFilter="QuickFilter" ReadOnly="false" EditMode="@DataGridEditMode.Inline">
    <ToolBarContent>
        <MudText Typo="Typo.h6">@ListPanelText</MudText>
        <MudSpacer />
        <MudTextField @bind-Value="_searchString" Placeholder="Search for PlayerProfiles..." Adornment="Adornment.Start" AdornmentIcon="@Icons.Material.Filled.Search" IconSize="Size.Medium" Class="mt-0"></MudTextField>
    </ToolBarContent>
    <Columns>
        <Column T="PlayerOwnedPaidServiceToken" Field="Id">
            <EditTemplate>
                <MudFab @onclick="@(async () => await Delete(@context))" Color="Color.Primary" Icon="@Icons.Material.Filled.Delete" Size="Size.Small" IconSize="Size.Small" />
                @context.Id
            </EditTemplate>
        </Column>
        <Column T="PlayerOwnedPaidServiceToken" Field="OwnerId" />
        <Column T="PlayerOwnedPaidServiceToken" Field="ServiceTokenId" />
        <Column T="PlayerOwnedPaidServiceToken" Field="Origin">
            <EditTemplate>
                <MudSelect T="ConsumableOrigin" @bind-Value="context.Origin">
                    @foreach (ConsumableOrigin item in Enum.GetValues(typeof(ConsumableOrigin)))
                    {
                        <MudSelectItem Value="@item">@item.GetDisplayName()</MudSelectItem>
                    }
                </MudSelect>
            </EditTemplate>
        </Column>
        <Column T="PlayerOwnedPaidServiceToken" Field="Status">
            <EditTemplate>
                <MudSelect T="ConsumableStatus" @bind-Value="context.Status">
                    @foreach (ConsumableStatus item in Enum.GetValues(typeof(ConsumableStatus)))
                    {
                        <MudSelectItem Value="@item">@item.GetDisplayName()</MudSelectItem>
                    }
                </MudSelect>
            </EditTemplate>
        </Column>
        <Column T="PlayerOwnedPaidServiceToken" Field="Period">
            <EditTemplate>
                <MudDatePicker @bind-Date="context.Period" />
                <MudTimePicker Time="context.Period?.TimeOfDay" TimeChanged="(t) => context.Period = context.Period?.Date + t" />
            </EditTemplate>
        </Column>
        <Column T="PlayerOwnedPaidServiceToken" Field="UsedDate">
            <EditTemplate>
                <MudDatePicker @bind-Date="context.UsedDate" />
                <MudTimePicker Time="context.UsedDate?.TimeOfDay" TimeChanged="(t) => context.UsedDate = context.UsedDate?.Date + t" />
            </EditTemplate>
        </Column>
        <Column T="PlayerOwnedPaidServiceToken" Field="InvalidateDate">
            <EditTemplate>
                <MudDatePicker @bind-Date="context.InvalidateDate" />
                <MudTimePicker Time="context.InvalidateDate?.TimeOfDay" TimeChanged="(t) => context.InvalidateDate = context.InvalidateDate?.Date + t" />
            </EditTemplate>
        </Column>
        <Column T="PlayerOwnedPaidServiceToken" Field="ExpireDate">
            <EditTemplate>
                <MudDatePicker @bind-Date="context.ExpireDate" />
                <MudTimePicker Time="context.ExpireDate?.TimeOfDay" TimeChanged="(t) => context.ExpireDate = context.ExpireDate?.Date + t" />
            </EditTemplate>
        </Column>
    </Columns>
    <PagerContent>
        <MudDataGridPager T="PlayerOwnedPaidServiceToken" />
    </PagerContent>
</MudDataGrid>

@code {
    bool _editPanelExpanded = false;
    string _searchString = "";
    PlayerOwnedPaidServiceToken _newItem = new();

    static readonly string EditPanelId =  $"{nameof(PlayerOwnedPaidServiceToken).ToLower()}-edit-panel";
    static readonly string EditPanelText = $"Add {nameof(PlayerOwnedPaidServiceToken)}";
    static readonly string ListPanelText = nameof(PlayerOwnedPaidServiceToken);

    private bool QuickFilter(PlayerOwnedPaidServiceToken x)
    {
        if (string.IsNullOrWhiteSpace(_searchString)) return true;

        if ($"{x.Id} {x.Origin} {x.Status} {x.Period} {x.UsedDate} {x.InvalidateDate} {x.ExpireDate}".Contains(_searchString)) return true;

        return false;
    }

    async Task OnValidSubmit(EditContext contex)
    {
        dbContext.Add(_newItem);
        await dbContext.SaveChangesAsync();
        snackBar.Add($"New {nameof(PlayerOwnedPaidServiceToken)} (id:{_newItem.Id}) Created.", Severity.Success);
        _newItem = new ();
    }

    void Clear()
    {
        _newItem = new();

    }

    async Task Delete(PlayerOwnedPaidServiceToken x)
    {
        var parameters = new DialogParameters();
        parameters.Add("ContentText", $"Do you really want to delete this records? This process cannot be undone.");
        parameters.Add("ContentSubText", $"{x}");
        parameters.Add("ButtonText", "Delete");
        parameters.Add("Color", Color.Error);

        if (!(await dialogService.Show<ConfirmDialog>("Delete", parameters, new DialogOptions() { CloseButton = true, MaxWidth = MaxWidth.ExtraSmall }).Result).Cancelled) {
            dbContext.PlayerOwnedPaidServiceTokens.Remove(x);
            await dbContext.SaveChangesAsync();
            snackBar.Add($"{nameof(PlayerOwnedPaidServiceToken)} (id:{x.Id}) Deleted.", Severity.Success);
        }
    }
}
